version: '3.8'

services:
  tracksolid-debugger:
    build: .
    container_name: tracksolid-debugger
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
